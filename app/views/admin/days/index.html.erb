<h1>Расписание</h1>

<table class="table">
  <thead>
    <tr>
      <th>День</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @days.each do |day| %>
      <tr>
        <td><%= link_to day, [:admin, day] %></td>
        <td>
          <%= link_to 'Изменить', edit_admin_day_path(day), class: 'btn btn-default' %>
          <%= link_to 'Удалить', [:admin, day], method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
